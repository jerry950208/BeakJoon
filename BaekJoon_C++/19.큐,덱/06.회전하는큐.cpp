/*
	회전하는 큐

	N개의 원소를 포함하고 있는 양방향 순환 큐가 있다. 여기서 몇 개의 원소를 뽑아내려고 한다
	이 큐에서 3가지 연산을 수행할 수 있다
	1. 첫 번째 원소를 뽑아낸다(A1 --- Ak 였던 원소가 A2 --- Ak 가 된다)
	2. 왼쪽으로 한 칸 이동시킨다 (A1 --- Ak 였던 원소가 A2 --- Ak, A1 이 된다)
	3. 오른쪽으로 한 칸 이동시킨다 (A1 --- Ak 였던 원소가 Ak, A1 --- Ak-1이 된다)
*/
#include <iostream>
#include <queue>
using namespace std;

int main(void){
	
	queue<int> q;
	int N, M, cnt;
	cin >> N >> M;
	
	
}
